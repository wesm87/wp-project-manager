'use strict';

import log from '../include/log';

module.exports = {
	command:  'wp install',
	describe: 'install WordPress',
	builder:  {},
	handler() {
		log.error( 'This feature is not ready' );
	},
};
