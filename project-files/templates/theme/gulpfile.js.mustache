
// Do NOT edit this section.
require.main.require = require;
process.env.gulpDir  = require( 'path' ).resolve( '../blr-base-theme/gulp' );

// Custom config for each task.
global.gulpConfig = {
	js: {
		libs: {
			admin: [

			],
			frontend: [

			],
		},
	},
};

/**
 * Imports all of the default tasks provided by the base theme:
 *
 *   default
 *   watch
 *   release
 *   build
 *   build/css
 *   build/js
 *   build/images
 *   build/fonts
 *   docs
 *   docs/sass
 *   docs/js
 *   docs/php
 *   qa
 *   qa/lint
 *   qa/test
 *   qa/travis
 */
// eslint-disable-next-line no-unused-expressions
require( process.env.gulpDir ).tasks;

// Add your custom tasks below:
// const gulp = require( 'gulp' );
