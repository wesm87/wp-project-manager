
// Do NOT edit this section.
require.main.require = require;
process.env.gulpDir  = require( 'path' ).resolve( '../blr-base-theme/gulp' );

/**
 * Imports all of the default tasks provided by the base theme:
 *
 *   default
 *   watch
 *   build
 *   build/css
 *   build/js
 *   build/images
 *   build/fonts
 *   docs
 *   docs/sass
 */
require( process.env.gulpDir );

// Add your custom tasks below:

const gulp = require( 'gulp' );
